from odoo import api, fields, models, _


class LeavesReportingLogic(models.Model):
    _inherit = 'hr.leave.report'

    # @api.model
    # def search(self, args, offset=0, limit=None, order=None, count=False):
    #     # Add your default filter conditions
    #     args += [('state', '=', 'validate')]
    #     return super(LeavesReportingLogic, self).search(args, offset, limit, order, count)




